[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.mysqli", "php82Extensions.session"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader 2>/dev/null || true"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t ."
